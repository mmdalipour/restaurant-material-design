import React from "react";
import { makeStyles } from "@material-ui/styles";

// components

// styles
const useStyles = makeStyles(() => ({}));

const Hero = () => {
  return <></>;
};

export default Hero;
